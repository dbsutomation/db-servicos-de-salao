
import { Client, Service, ServiceRecord, TeamMember } from '@/types';

// Deixamos o arquivo vazio, pois agora estamos buscando os dados diretamente do Supabase
// Apenas exportamos tipos vazios para não quebrar importações existentes

export const teamMembers: TeamMember[] = [];
export const services: Service[] = [];
export const clients: Client[] = [];
export const serviceRecords: ServiceRecord[] = [];
